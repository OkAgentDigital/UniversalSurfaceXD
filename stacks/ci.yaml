name: universui-ci
version: 1.3.0
description: Universui for GitHub Actions and CI/CD pipelines
author: DevStudio
license: MIT

components:
  - name: core
    source: "@universui/core"
    version: ^1.3.0
    required: true
    config:
      mode: ci

  - name: sonic-vault
    source: "@universui/sonic-vault"
    version: ^1.0.0
    config:
      use_env_vars: true

  - name: sonic-run
    source: "@universui/sonic-run"
    version: ^1.0.0
    config:
      container_runtime: docker
      mcp_servers:
        - github

  - name: sonic-agent
    source: "@universui/sonic-agent"
    version: ^1.0.0
    config:
      ci_mode: true
      agent: github-actions

excluded_components:
  - workbench-ui
  - sonic-chasis
  - sonic-logs

build:
  output: universui-ci
  target_os: [linux]
  target_arch: [amd64]
  static_linking: true

runtime:
  min_memory_mb: 128
  recommended_memory_mb: 512
  no_gui: true
  ephemeral: true

max_size_mb: 20
actual_size_mb: 15
